import * as THREE from "three";
//// color name model 3 Capot001_CAR_PAINT_0
/// color name cyber truck Object_4
const state = {
  activeMesh: {},
  activeMeshName: "Capot001_CAR_PAINT_0",
  cameraPos: new THREE.Vector3(9, 2, 4),
  target: new THREE.Vector3(5, 0, 0),
  shouldUpdate: true,
};

export default state;
